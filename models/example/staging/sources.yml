version: 2

sources:
  - name: stripe
    schema: dbo

    tables:
      - name: customers
        columns:
          - name: ID
            data_tests:
              - unique
              - not_null
              
      - name: orders
        columns:
          - name: ID
            data_tests:
              - unique
              - not_null
              

      # Check for stale sources (Freshness)
      
        # config: 
          # freshness:
          #   warn_after:
          #     count: count
          #     period: period
          #   error_after:
          #     count: err_count
          #     period: err_period
          # loaded_at_field: loaded_at_field
      - name: payments
        columns:
          - name: ID
            data_tests:
              - unique
              - not_null